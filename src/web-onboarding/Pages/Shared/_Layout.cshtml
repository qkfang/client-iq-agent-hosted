<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Customer Onboarding Portal</title>
    <link rel="stylesheet" href="~/css/site.css" />
</head>
<body>
    <header class="app-header">
        @{
            var currentPage = ViewContext.RouteData.Values["page"]?.ToString();
        }
        <nav class="top-nav">
            <a asp-page="/Index" class="brand"><span class="brand-mark">O</span>Customer Onboarding Portal</a>
            <ul class="nav-links">
                <li><a asp-page="/Index" class="@(currentPage == "/Index" || currentPage == "/Details" ? "active" : "")">Customers</a></li>
                <li><a asp-page="/Onboard" class="@(currentPage == "/Onboard" ? "active" : "")">Onboard Customers</a></li>
            </ul>
            <span class="user-area">
                @if (User.Identity?.IsAuthenticated == true)
                {
                    <span class="user-name">@User.Identity.Name</span>
                    <a asp-area="MicrosoftIdentity" asp-controller="Account" asp-action="SignOut">Sign out</a>
                }
                else
                {
                    <a asp-area="MicrosoftIdentity" asp-controller="Account" asp-action="SignIn">Sign in</a>
                }
            </span>
        </nav>
    </header>
    <main role="main">
        @RenderBody()
    </main>
    <footer class="app-footer">
        <span>&copy; @DateTime.UtcNow.Year Customer Onboarding Portal</span>
        <span>Powered by Microsoft Foundry</span>
    </footer>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
