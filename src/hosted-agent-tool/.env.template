# =============================================================================
# Microsoft Foundry project (local dev only — hosted deployment injects these)
# =============================================================================
# Your Foundry project endpoint (Foundry portal -> project -> Overview)
FOUNDRY_PROJECT_ENDPOINT=https://aisa-ciquocsj.services.ai.azure.com/api/projects/aifp-ciquocsj

# Model deployment name — must match a model deployed in your Foundry project
AZURE_AI_MODEL_DEPLOYMENT_NAME=gpt-5.6-sol

# =============================================================================
# Fabric IQ tool (optional — leave blank to deploy without it)
# =============================================================================
# Full ARM resource ID of the Fabric IQ project connection
FABRIC_IQ_CONNECTION_ID=
# Override only when using a non-default Fabric IQ MCP endpoint
FABRIC_IQ_SERVER_URL=
